import React from 'react';
import { connect } from 'react-redux';

const mapStateToProps = () => ({
});

const mapDispatchToProps = (dispatch) => ({
    
});

const ConMenu = connect(mapStateToProps, mapDispatchToProps)();

export default ConMenu;